# Quality scorecard

Status: canonical | Owner: quality | Last verified: 2026-08-14

The commit each verdict was measured against is **not written here**. It lives in the run records
under `.evidence/`, which is where `gate:evidence` reads it, and duplicating it in this header is
how it went stale before: a hand-copied SHA has no gate behind it and drifts on the next commit.
Run `npm run gate:evidence` to see the head every row is currently bound to.

## How to read this

A row states what is **proven by automated tests in this repository, measured locally**, and names
what is still missing behind it. "Done" in this repository requires behaviour, negative
authorization, visible error state, observability, automated acceptance, documentation linkage, and
clean CI. No row below satisfies the last of those, so no row says "done".

Three terms are used precisely:

- **Local** — measured on a developer machine by one of the commands in
  [what was measured](#what-was-measured), on the date that section names. Every verdict here is
  local.
- **Hosted CI** — a GitHub Actions run whose conclusion can be inspected. **No hosted CI run exists
  for any commit on this branch.** The most recent green run of all five jobs (`integrity`,
  `test-build`, `d1`, `browser`, `security`) is run `31471037575` at head `10eab436`. That head and
  this branch are **divergent** — neither is an ancestor of the other, so the branch is neither
  "ahead of" nor "behind" it. Measured on 2026-08-11: `git merge-base HEAD 10eab436` answers
  `fd21987`, and `git rev-list --left-right --count 10eab436...HEAD` answers one commit on the run's
  side and five on this branch's. The green run carries one commit this branch does not have
  (`10eab43`, the last commit of pull request #88, from whose branch this one was cut one commit
  earlier), and this branch carries five commits that run never saw. Treat hosted CI as pending, not
  as passed.
- **Verdict (local)** — a statement about the **journey** the row names, not about the competition
  feature that journey serves. *shipped* means every step of that journey is exercised by an
  automated test on this machine; *partial* means a step of it has no automated exercise, or has no
  implementation to exercise. A journey can be shipped while the brief feature it serves is partial —
  `ACC-CFP` and `ACC-REVIEW` are exactly that — because feature completeness is a different
  question, answered in its own column in the
  [traceability table](../product/competition-traceability.md).

## What was measured

Everything below was run on 2026-08-11, in this order, against the commit this document names.
Each run wrote a record under `.evidence/`, and `gate:evidence` refuses these rows if a record is
missing, failed, or was produced against a different commit — so the numbers here are checkable
rather than asserted.

The browser commands used to name ports `4373`/`9087` because another checkout on this machine
was holding the defaults. They no longer need to: ports are derived per checkout, and a server
belonging to a different one aborts the run instead of being silently tested (`GAP-004`,
issues #28 and #90).

| Command | Result |
|---|---|
| `npm run check` | exit 0 — `gate:integrity` (gate drift over 6 gates, Biome/Ruff format, `greenroom-context check`, Python CLI tests, lint + AST error policy, typecheck, OpenAPI drift, declared-schema drift over 34 tables and 22 migrations), then `gate:test-build` (376 tests across the `node --test` tool suite, `@greenroom/api` and `@greenroom/web`, plus both production builds), then `gate:d1` (28 tests in 12 files), then `gate:evidence` |
| `npm run gate:browser` (`setup:local`, production web build, `reset`, then the suite) | 30 passed, on derived ports with no manual port assignment. This is the clean-reset run; building first also proves Wrangler can serve the production frontend artifact from a clean checkout |
| `npm run test:e2e` again against the same still-running servers | 30 passed — re-runnable on one fixture without a reset |
| `npm run test:quality` | 3 passed |
| `npm run gate:security` | exit 0 — `npm audit --audit-level=high` found 0 vulnerabilities |
| gitleaks | **not runnable locally.** It is a marketplace action; it succeeded in the `security` job of run `31471037575` at head `10eab436` |

The earlier measurement of two `wrangler dev` instances of one worktree corrupting each other —
2 failures across 5 runs, a different spec each time — is **no longer reproducible and is not
re-stated as current**: local D1 and R2 state is now keyed per instance, so the two processes no
longer share a database (issue #28). The record of why that changed is in
[known gaps](known-gaps.md) under `GAP-004`.

**No row below has its own command, because no such command exists.** An earlier version of this
table gave each row an `npm run test:e2e … -- e2e/<one>.spec.ts` line and called it that row's
clean-reset reproduction. That was wrong twice over: none of those commands had been run, and a
single-spec invocation is not equivalent to the evidence above. The browser specs share one mutable
local D1 fixture at `workers: 1` and are deliberately order-dependent — `00-seed-state.spec.ts` is
named to sort first precisely so that `cfp.spec.ts` cannot repair the seed before it is asserted —
and outside CI Playwright reuses any server already answering, so a single-spec run against running
servers performs **no reset at all**. Measured on 2026-08-11, this command:

```bash
GREENROOM_WEB_PORT=4373 GREENROOM_API_PORT=9087 npm run test:e2e --workspace @greenroom/web -- e2e/agenda.spec.ts
```

passed 6 tests in 5.4s against the already-running servers and the fixture the two full runs above
had left behind, resetting nothing. It is a debugging tool. What reproduces a row is the whole suite
from a reset, in the order above.

The **Automated evidence** column therefore names the test files that carry each row's `@acceptance`
marker. Each of them is run by `npm run check` (tool, API, and component suites), by `npm run test:d1`
(`*.integration.test.ts`), or by the full `npm run test:e2e` (`e2e/*.spec.ts`).

## What makes a row here checkable

Every verdict below is bound to a run, not asserted in prose.
[`acceptance-evidence.json`](acceptance-evidence.json) states, per row, which suites it rests on
and which spec files carry its marker; each suite writes a record under `.evidence/` naming its
exit status, counts, and the commit it ran against; and `npm run gate:evidence` — the fourth gate
in `npm run check` — fails a row whose suite has no record, has a record from a different commit,
has a record of a failing run, or names a spec file that no longer exists.

That last rule exists because of a specific failure. `ACC-AGENDA` claimed complete Playwright
evidence for the agenda resource editor in the same change that deleted its only browser
coverage (#87), and nothing connected the two. It is now a build failure. So is the older and
more expensive case: the `ACC-CFP` row read "passed locally 2026-08-10 … complete" while every
public proposal submission returned 500 from a clean reset, and the row is *why* the defect
survived — the documentation said the journey worked.

The freshness rule is deliberate and narrow: a record counts only for the commit it names.
A suite that passed on other code is not evidence about this one. It does **not** detect
uncommitted working-tree changes — the binding is to `HEAD`, not to a tree hash — so a record
made before an edit still satisfies the gate until the next commit. Producing evidence is
therefore the last step before publishing, not the first.

## Acceptance rows

| Acceptance ID | Journey | Verdict (local) | Automated evidence | Not covered by this row |
|---|---|---|---|---|
| `ACC-HARNESS` | reference slice | shipped | `tools/tests/check-errors.test.mjs`, `check-gate-drift.test.mjs`, `check-schema-drift.test.mjs`, `tools/tests/test_context.py`; `apps/api/test/http.test.ts`, `api-compatibility.test.ts`, `demo-session.test.ts`, `runtime-auth.test.ts`, `event-service.test.ts`, `event-mappers.test.ts`, `d1-event-repository.integration.test.ts`, `api-clients.test.ts`, `d1-api-clients.test.ts`, `d1-api-clients.integration.test.ts`; `apps/web/test/router.test.tsx`, `error-fallback.test.tsx`, `api-clients-api.test.ts`, `api-clients-workspace.test.tsx`. API-client coverage asserts one-time hashed credentials, creator-grant and event-allowlist intersection, organization-scoped event creation, service and atomic D1 rotation, bounded listing-query count, D1 persistence, concurrent revoke, unkeyed revoke convergence, keyboard-operated credential copy, and that the browser sends no decorative retry key for create, rotate, or revoke. Compatibility coverage asserts one version constant drives OpenAPI and success/refusal/not-found response headers, and that the shared cursor factories retain communications history's existing wire shape and bounds. The deprecation policy is written; **no endpoint has actually been deprecated**, so this row claims no exercised `Deprecation` or `Sunset` rollout. Webhook key-scoped replay is evidenced under `ACC-INTEGRATION`; API-client create/rotate are the documented hash-only security exception and revoke claims natural convergence rather than key semantics. **No browser spec carries this marker** — an earlier version of this row cited `apps/web/e2e/reference-slice.spec.ts`, which carries `ACC-IDENTITY-EVENTS` and `ACC-AGENDA` | Branch protection is **not enabled**: `gh api repos/:owner/:repo/branches/main/protection` answered 404 "Branch not protected" on 2026-08-11, so the five CI jobs are not required checks (`GAP-003`) |
| `ACC-IDENTITY-EVENTS` | identity/event foundation, `JNY-010` | shipped | `apps/web/e2e/reference-slice.spec.ts`; `apps/web/test/App.test.tsx`, `landing.test.tsx`; `apps/api/test/d1-identity-directory.integration.test.ts`, `d1-identity-signup.integration.test.ts`, `d1-identity-sessions.integration.test.ts`, `identity-sessions-http.test.ts`, `real-auth.test.ts`, `google-oauth.test.ts`, `signup-service.test.ts`, `oauth-attempt-cookie.test.ts`, `demo-reset-guard.integration.test.ts`, `d1-event-repository.integration.test.ts`, plus the session and authorization tests listed under `ACC-HARNESS` | **Google sign-in and self-serve signup ship here, and the security properties are asserted rather than described.** `google-oauth.test.ts` signs its own tokens with a generated RSA key and refuses: a token signed by another key, `alg: none` and the HS256 key-confusion variant, a `kid` absent from the published set, another client's audience, a non-Google issuer, an expired token, and a mismatched nonce — and asserts the authorization URL carries an S256 challenge that is exactly the digest of the verifier it kept, which never leaves the server. `signup-service.test.ts` covers link-versus-provision, including that a seeded speaker who signs in with Google stays a speaker, and that an **unverified** address is refused before any directory call. `d1-identity-signup.integration.test.ts` proves single use of an attempt against real D1 (`DELETE … RETURNING`), and carries the demo-isolation proof: driven through `D1IdentityDirectory` and `EventService` composed as `index.ts` composes them — not through the in-memory demo fixture, which has drifted narrower than the D1 projection and would prove less — a demo persona holds no membership of and no role on a self-serve organization or its event, each actor's `list()` excludes the other's event, and `get()` answers null for it. Every one of those was confirmed by breaking the implementation and watching the test fail. **Sign-out is now revocation, and the proof is in the negative cases.** An issued session is a row; `d1-identity-sessions.integration.test.ts` drives the store against real D1 and asserts that a revoked row stops resolving, that an expired one does too, that revoking twice is zero rows rather than an error or a fabricated count, and that `revokeAllForUser` ends both of one organizer's sessions and neither of a reviewer's — the mutation for that last one is dropping the `WHERE user_id` predicate. `identity-sessions-http.test.ts` asserts the routes: the same cookie is refused on the request after sign-out, sign-out answers byte-identically with a valid session, a forged one and none at all, revoke-all reports the caller's own count and refuses an unauthenticated caller, and **a demo persona takes zero session-store reads across its whole request and is refused revoke-all**. `real-auth.test.ts` asserts that the signature is verified before any store read (a wrong signature, a wrong secret and an expired payload each cost no lookup), that a pre-change token carrying no `sid` is refused, that a record naming another user is refused rather than followed, that a bearer token dies with the session it was minted from, and that the token is still exactly two dot-separated parts — the invariant that lets one cookie name carry a persona and a real session. Nine mutations were applied and each was caught by its named test. **Membership administration ships here too**, which is the largest piece of product #12 was missing: before it, no route in the repository wrote `organization_memberships` or `event_roles`. Invitations are accepted by the accepting session's own identity and never by matching the address they name — `membership-service.test.ts` asserts the repository is handed the caller's own id and a digest, and is never told the address — and `d1-identity-membership.integration.test.ts` proves single use against real D1 — a second acceptance of the same token answers null, and a second *caller* is granted nothing, which is the half a one-user test cannot see and the half the `accepted_by_user_id` guard exists for; that acceptance is one transaction, so a grant that cannot be written leaves the token unspent rather than consumed with no membership and no audit row; that a revoked and an expired invitation refuse indistinguishably; that a removal which removed nobody records nothing, the same guard the session store carries and for the same two reasons — a row claiming otherwise is a lie, and an authorized caller could otherwise append to an unpruned table at will; that removing a member takes their roles on this organization's events and leaves another organization's alone (mutation: drop the event-id predicate), and that a removed role is gone on the very next actor resolution with no session touched — which is why removal does not revoke sessions. The three demo-safety rules are enforced and audited: a seeded persona is refused as a grant subject, as an administering actor, and as an accepter, each with an `outcome = 'refused'` row. **The browser cannot drive the invite-and-accept journey**, and that is a consequence of the second rule rather than a missing test: the browser suite runs in demo mode, where every membership write is refused for being a persona. `members.spec.ts` asserts the surface renders and that the refusal is visible and carries its correlation reference; the journey itself is covered at D1 and transport level. `api-clients.spec.ts` asserts that a session-only organizer can create a client, sees the plaintext credential once, and cannot recover it after reloading the console. Authorization is the CRM directory's three-condition organization pattern on a new event-earned `identity:manage` capability, and as with the CRM the mixed-role escalation is asserted at service level only, because no seeded persona belongs to two organizations. **Credential rotation and recovery close the issue.** `SESSION_SECRET_PREVIOUS` gives rotation a window — issuance uses the current secret, verification tries current then previous — so rotating no longer signs everybody out at the instant of the deploy; `real-auth.test.ts` asserts a session, an emailed code and a demo persona cookie each minted under the previous secret still resolve, and that all three stop once it is unset, and `runtime-auth.test.ts` asserts the boot guard refuses a previous secret equal to the current one or set to the placeholder. `tools/revoke-sessions.mjs` is incident revocation, guarded by a `--confirm <worker-name>` that must match this checkout's config, by requiring exactly one of `--user` and `--all`, and by refusing any user id that is not a plain identifier — the id is interpolated into SQL because `wrangler d1 execute` takes no bound parameters, so it is refused rather than escaped; every one of those guards has a test and the happy path deliberately has none, because it runs `wrangler` against a live database. `docs/engineering/security-operations.md` is the runbook. **What none of that proves is a performed rotation**: no secret has been rotated on a deployed instance, and `revoke-sessions` has never been run against one — there is no deployed instance (`GAP-008`). The procedures are documented and their guards are tested; the operations themselves are unexercised. Two recovery cases are documented rather than implemented and say so: an address nobody controls has no self-serve path, and an organization whose last organizer removes themselves needs an operator to grant the role back, because nothing refuses that removal. The audit spine now writes its `membership.*` and `event_role.*` actions as well as the three `session.*` ones, and the Google callback's refusals are deliberately logged rather than audited (`ADR-005`). The provider ADR it also owed is `ADR-004`. **The live adapter has now exchanged requests with Google.** On 2026-08-14, at commit `d14b047` and against OAuth client `629474442220-…-ab3t4tb1bgddnsfjjkm801puan53chtu`, one real sign-in was completed on the deployed demo: the authorization request was accepted, the code exchanged at the token endpoint, and the `id_token` verified against Google's **published** key set rather than a generated one — all of which had only ever run against a stub. The deployed database corroborates it independently, holding one organizer with both a provider account and a membership beside the organization and first event that sign-in provisioned. No divergence from Google's documented behaviour was seen, so no case was added to `google-oauth-client.test.ts`. Google's own refusal paths — a rotated signing key, a retired `kid`, a replayed code, the key-cache expiry, the timeout — remain covered against a stub, which is where every other adapter also stands (`GAP-020`, issue #165). A hosted smoke remains in `GAP-008`. **The two first-sign-in races and the two-tab refusal are closed** (issues #164, #166): `d1-identity-signup.integration.test.ts` runs two genuinely concurrent `signInWithGoogle` calls for one new subject against real D1 and asserts the database holds exactly one organization, one event and one user beyond the seeded fixture — counted with the demo restore's own guard query, so the assertion is about the deployment rather than only about the test — with the single organizer role read separately through identity's own directory, and that the same file's provisioning case leaves no event without the role that opens it; that case fails on its first assertion against the pre-fix code. `signup-service.test.ts` adds the recovery: the losing callback's organization is discarded rather than orphaned, a provisioning failure that is not a race discards it too, and a discard that itself fails reports both errors — and one case that is not about races at all, because the review of this change found it: a person who accepts an **organization-level** invitation holds a membership and no event role, which is the same state a half-finished signup leaves, and adopting "the organization's first event" for them handed a new member `events:settings:update`, `agenda:manage` and `review:manage` on somebody else's event. That was pre-existing and is closed here, and it took three goes — recorded because the two rejected fixes are the instructive part. Completing a workspace now **provisions and never adopts**, and provisions only into an organization that holds no events *and* has no other member. Adopting "the organization's first event" was the original defect; keying that adoption to the organization did not help, because every self-serve organization carries a provisioned first event for ever, so a later member still adopted the owner's; and "no events" alone would have made a newcomer the organizer of somebody's empty organization while stranding its owner. Both halves of the condition are load-bearing and each is pinned by a mutation: deleting the events check fails `signup-service.test.ts`'s "does not restore an event role that was revoked", and deleting the member count fails its "provisions nothing into an empty organization somebody else already belongs to" — with `d1-identity-signup.integration.test.ts` driving both against real D1. Two consequences are deliberate and stated in the code: a revoked event role is not restored by signing in again, and a signup whose organization has since gained an event or a second member is not resumed — that person's membership already lets them create an event themselves. `http.test.ts` drives two starts and two callbacks in one browser and asserts the older tab's completion leaves the newer tab's attempt outstanding, that a callback from a browser holding no attempt never reaches the provider, and that a refusal spends none of the attempts a browser holds; `oauth-attempt-cookie.test.ts` covers the cookie's own edges. **`GAP-019` is closed**, so the demo reset now reads the data before it writes: `demo-reset-guard.integration.test.ts` runs the shipped guard against a real seeded database, proceeds on a clean fixture, refuses on one carrying a self-serve signup, and then applies `seed/reset.sql` to show the rows it refused to delete really do disappear. The first live reading also exposed a false-positive class in the `GAP-019` guard, and closed it. It had counted every non-seeded `users` row, but demo usage writes those too — accepting a proposal converts a speaker — so the deployed database read 1 organization, 1 event and **3** users, two of them speakers converted against the *seeded* event; a routine restore would then have needed the destroy flag after the demo had merely been *used*, which is the habit that flag exists to prevent. A row now counts when something attaches it to a person: a provider account, an organization membership, or an event role outside the seeded events — and an event counts only outside the seeded organizations, the same defect one table over, found by looking rather than by meeting it. `demo-reset-guard.integration.test.ts` drives both through the services production uses and pins them by mutation: reverting either makes demo usage count as somebody's data again, while a speaker holding a role in a self-serve workspace still counts |
| `ACC-REVIEW` | `JNY-003` | shipped | `apps/api/test/review-service.test.ts`, `review-http.test.ts`, `content-http.test.ts`, `d1-review-repository.integration.test.ts`; `apps/web/test/proposal-acceptance.test.tsx`; `apps/web/e2e/review-workflow.spec.ts`, `lifecycle.spec.ts` | Numeric, dropdown, and free-text criteria round-trip, numeric weights drive aggregates, capped distribution scales assignment, reviewer progress identifies outstanding work, and organizer results sort and export as CSV with criterion values. Co-author role labels are projected when CFP supplies the structured `coauthors` answer. Round advancement preserves the same reviewer's earlier assignments, evaluations, and round-scoped outcomes. Bulk reminder delivery remains blocked by the missing reviewer-reminder communications trigger (`GAP-010`, issue #66); review does not substitute `reviewer.assigned` or write communications tables. **AI-assisted review now exists, draft-only (issue #110).** A provider-neutral suggestion port drafts a summary and one value per rubric criterion, each with a rationale, stored with the model, prompt version, time and abstract revision it came from and rendered with all four beside the draft. The deterministic fake is the default everywhere — `npm run check`, the D1 suite, Playwright and the demo need no credential and no network — and a credential-gated Anthropic adapter sits behind `REVIEW_AI_PROVIDER=live`, which refuses a missing key rather than falling back to the fake. **The draft-only guarantee is structural rather than conventional**, and the evidence is deliberately made of negatives: `review-suggestions.test.ts` asserts that drafting writes no evaluation, no outcome and no decision, and that *accepting* leaves `review_outcomes` empty until the reviewer separately completes; `d1-review-repository.integration.test.ts` drives the two storage guards against real D1 — a suggestion cannot leave `offered` without a named responder, and an evaluation cannot claim a suggestion belonging to another assignment or reviewer — and proves a replayed acceptance does not overwrite scores the reviewer has since edited. Blind review holds across the port: the request type has no field for a submitter and the service passes the masked projection, asserted by serializing what the fake received. Degradation is asserted at three levels rather than described: the service (a hanging provider times out and the reviewer completes by hand), the route (`502 UPSTREAM_UNAVAILABLE` carrying a normalized code, then a successful evaluation `PUT`), and the rendered card (`review-suggestions.test.tsx`). The port can be switched `off`, and that state is covered too — no Draft control, `404` on the route, the rest of the journey unchanged. `review-workflow.spec.ts` carries both halves in the browser: drafting shows the provenance and leaves the scoring form empty, dismissing records nothing, and accepting fills the reviewer's own controls while the queue still reads "Draft saved" rather than "Completed". **The live path is now verified.** The 2026-08-13 staging smoke at commit `83c757389a2468500172fc2a5f7aeeeb46497345` covered deployed fail-safe and manual scoring, schema-valid `claude-opus-5` generation and provenance, D1 persistence, accept-as-draft with no outcome, separate completion, revoked-key and safety-refusal normalization, and inspection of the identity-free outbound request (`GAP-011` closed). No credential exists here and deterministic tests continue to stub the provider boundary. **Confirming an acceptance is now measured, and the measurement is a gate (issue #207).** A person on the deployed demo reported it taking a few seconds; the cost is a serialized chain of round trips to D1, which is the unit `acceptance-latency.integration.test.ts` counts over the real services against a migrated, seeded database with the lifecycle ports composed the way `index.ts` composes them. Locally D1 is a SQLite file and a statement costs microseconds, so wall-clock here says almost nothing about a deployment while the count says the same thing in both. One acceptance went from **65 sequential waits to 35**: the content workspace read issues its seven independent queries together rather than one after another, the “has this speaker any work yet” question is a one-row existence check rather than a read of nine tables, the composed route calls `acceptSession` and so stops producing a projection it discarded, and the composition root memoizes the owning organization for the life of one request where eight announcements each resolved it again. A review pass took five of those waits back and they were worth taking: the three announcements had been issued as one wave, and because a delivery's `created_at` is stamped inside its own enqueue while the sender claims deliveries ordered on exactly that column, the invitation could be queued behind the task notices it explains — so the invitation now precedes them and only the two task notices, which nothing orders against each other, go together. **The atomicity the issue forbids weakening is untouched** — the decision and the session are still one composed request whose failure between them leaves a repairable `decision_only` row — and notification behaviour is asserted rather than assumed: one invitation, two task notices and one decision notice per acceptance, and a repeat queues nothing. **A perceptual change was built and backed out**, which #207 asks to be told about either way: the console can announce from the response it already holds and refresh in the background, but `setBusy(false)` then re-enables every control — and unguards the dialog — over a table that still shows the abstract undecided, the bulk dialog reaches `done` before the rows it annotates exist, and `useLoad`'s polite "Updating abstract triage…" status speaks over the confirmation. The saving was one round trip whose server side measures two sequential D1 waits, on top of a request already cut from 65 to 35. Three further costs were measured and deliberately not taken, two because they need a change to another domain's port and one because it is inherent — the speaker conversion's twelve round trips are claim-then-read-who-won pairs, which is the mechanism that makes two concurrent conversions land on one speaker, and they are the cold path only. |
| `ACC-CFP` | `JNY-001`, `JNY-002` | shipped | `apps/api/test/cfp-service.test.ts`, `cfp-submitter.test.ts`, `cfp-draft-isolation.test.ts`, `cfp-http.test.ts`, `cfp-submitter-http.test.ts`, `d1-cfp-repository.integration.test.ts`, `d1-cfp-account-binding.integration.test.ts`, `seed-state.integration.test.ts`; `apps/web/test/cfp-composer.test.tsx`, `cfp-window.test.tsx`, `cfp-submitter-dashboard.test.tsx`; `apps/web/e2e/00-seed-state.spec.ts`, `cfp.spec.ts`, `cfp-submitter.spec.ts`, `lifecycle.spec.ts` | Conditional visibility and category-based status routing are persisted and enforced on both client and server; resolved routes survive form edits in the submission snapshot (`GAP-009` closed by issue #49). Editable draft saves compare the loaded version atomically, preserve the winning draft on conflict, and offer an explicit reload path (issue #20). The seed defect this row once hid — a published snapshot with no `fields`, which rendered an empty public form and answered 500 — remains asserted before any spec can repair it, by `apps/web/e2e/00-seed-state.spec.ts` and `apps/api/test/seed-state.integration.test.ts`. **Issue #190 added the account-bound lifecycle**: a scheduled window whose precedence is one function (`cfpEffectiveState`, both gates must permit) and whose deadline is an instant rendered in the event timezone; proposals owned by a `submitter_user_id` with drafts, revisions under an optimistic `revision` guard, and a dashboard that shows accept/decline without exposing a triage status; a submission confirmation whose recipient is resolved from the session rather than from a form answer; and a decision message addressed at the owning account or not at all — the form answer is reached only for a proposal that has no account, through one rule stated in the communications domain (`lifecycleRecipient`), which bounds `#132`'s exposure to guest submissions rather than all of them. The browser journey drives sign-in from the public call, a title-only draft, a sign-out, a resume, a submission, two-account isolation, and a decision. Storage-level claims are separately pinned: a draft is invisible to all four read paths of the organizer/reviewer projection — asserted both behaviourally and by a source scan that fails when a fifth path is added without the predicate — four migration-`1201` triggers refuse an unowned draft, a lifecycle/status disagreement, an unsubmit and an owner reassignment, and the open-window guard inside every proposal write refuses a call closed between a request's read and its write. **Not covered by this row**: the *accepted* decision is driven end-to-end in the browser by `lifecycle.spec.ts` rather than by `cfp-submitter.spec.ts`, which declines instead — accepting creates a content session on the demo event and `lifecycle-demo.spec.ts` asserts that event's exact session count; the accepted → dashboard mapping is asserted in the D1 suite and the component suite. No message leaves the process in any suite: `COMMUNICATIONS_PROVIDERS` is unset, so `DeterministicProvider` marks every delivery sent, and the confirmation's *content* and *recipient* are asserted against delivery history rather than against a mailbox. Guest submission remains anonymous by construction and there is no claiming path, and a guest decision still goes to the unverified form address, so `#132` narrows rather than closes (`DEBT-013`'s enrichment guard is unchanged and still binding) |
| `ACC-SPEAKER` | `JNY-004`, `JNY-005` | shipped | `apps/api/test/content-service.test.ts`, `content-http.test.ts`, `content-resource-sanitizer.test.ts`, `content-csv-import.test.ts`, `content-deliverables.test.ts`, `d1-content-repository.integration.test.ts`; `apps/web/test/speaker-photo.test.tsx`, `proposal-acceptance.test.tsx`, `content-operations.test.tsx`, `speaker-checklist.test.tsx`; `apps/web/e2e/speaker-portal.spec.ts`, `speaker-checklist.spec.ts`, `lifecycle.spec.ts`, `lifecycle-demo.spec.ts` | Issue #144 inverted the workspace's information architecture: the dashboard leads, and authoring, imports and history sit below it as disclosures that are closed until opened. Measured on the seeded fixture at 1440px, the page went from **4475px to 2046px**, and the Accepted sessions table from ~1420px down the page (32%) to y=341, inside the first screen. `content-operations.test.tsx` holds the three claims a screenshot cannot: an audit row names a person rather than the id it stores them under, the Speaker workflow panel renders one form for a twelve-speaker roster where it used to render one per speaker without bound, and every tool is closed until asked for. What it does not assert is the height itself — `lifecycle-demo.spec.ts` asserts the ordering (the sessions table above the authoring tools, and within the first viewport), which is the property that matters and does not move with the fixture's size. Issue #62's headshot criteria remain asserted end to end. Issues #54 and #94 add organizer-authored sanitized resource pages, CSV preview/import with explicit invalid and duplicate rows, persistent workflow/custom fields, bulk dated tasks, task-associated versioned uploads with readable prior versions, attributed cross-role comments, attributed profile/session revisions with restore, and deterministic latest-only ZIP download. Issue #116 makes that revision and the canonical write it describes one D1 batch under a shared compare-and-swap guard, and moves revision numbering into the write; `d1-content-repository.integration.test.ts` asserts six behaviours against real D1 rather than a fake. A profile update a `CHECK` constraint refuses leaves no revision claiming it happened; two concurrent organizer edits are numbered `1` and `2` with the replaced state readable in the second revision's snapshot; a restore racing an edit produces three consecutive revisions whose last writer owns the session; a row deleted between an edit's read and its write answers absent and leaves nothing behind, rather than reporting the zero-row `UPDATE` D1 calls a success; a headshot chosen during an organizer's edit is neither reverted nor missing from the snapshot that edit records; and a writer that never wins the row raises `409 CONFLICT` having changed nothing. **Each was confirmed by breaking the implementation first** — removing the batch leaves the orphan revision, removing the retry refuses the second edit, removing the existence guard resurrects the deleted row's revision, removing the column guard erases the headshot — so these assert behaviour rather than the absence of a crash. **What that does not give is a merge.** Two edits to the same field still resolve last-writer-wins; the earlier value survives in history rather than in the profile, and no screen tells the losing organizer that happened. The `409 CONFLICT` is contention rather than a stale-edit signal, it is now declared on the four content routes that can raise it, and no console surface renders it — an editor-level precondition like the CFP composer's would be a product decision about the speaker portal, not a repair to this one. No production path rewrites a whole profile row from a copy it read any more: an edit goes through the guarded revision write, a headshot and a CSV import each write only the columns they own, and `updateProfile` survives for fixtures with a comment saying so. That is narrower than "an import cannot overwrite an organizer": an import and the organizer's workflow editor own the same three columns, so an import still lands over a workflow edit made while it ran, unguarded and recording no revision — the console's history then shows the organizer's value as the latest revision while the row holds the CSV's. What changed is that it no longer reverts the name, bio or headshot it never touched. **Issue #176 gives the speaker checklist a console surface, which it shipped without.** `speaker_task_templates` had commands, routes, contracts, a seed and a template slice that clones it, and no way for anybody using the product to write one — the category cloned correctly only because the seed had populated it. The content workspace now has a Speaker checklist panel that adds, edits, reorders and removes lines, and assigns the checklist to selected speakers as a separate, deliberate act. Two commands were added rather than reusing the bulk declaration, and the reason is the trap it would otherwise be: that path writes at `(event_id, title)`, so a corrected title creates a *second* line and leaves the mistyped one in the checklist for ever, since nothing there removes anything. Authoring therefore addresses the row — `POST /api/events/{eventId}/speaker-task-template-entries`, `PATCH` and `DELETE /api/speaker-task-templates/{templateId}` — and a title the checklist already holds is a 409 naming the field rather than a silent overwrite of the line still on screen. Removing a line leaves work already assigned from it alone, because `speaker_tasks` holds no pointer back and once assigned the work is that speaker's; `content-service.test.ts` asserts exactly that. `d1-content-repository.integration.test.ts` asserts the rename and both duplicate-title refusals against real SQLite rather than a double that could be more permissive than the column. `speaker-checklist.spec.ts`, whose authoring half writes a checklist on an event the run creates — never the demo event, whose seeded three lines every other spec and the demo itself read — correcting a typo, meeting the duplicate refusal and pruning a line. Its assignment half has to use the demo event, because that is the only place with a speaker to assign to: it adds one line named for the run, assigns, asserts that work was written and that a second press says everybody already has every line, then removes the line so the demo checklist ends where it started. The count is asserted as a number rather than pinned at one, deliberately — which seeded lines a speaker already holds is fixture history, and a suite that passes only against a freshly reset database is one nobody can re-run while debugging it. **What is not covered**: reordering is a number an organizer types rather than a drag, and there is no bulk import of a checklist from the console — the API's declaration route still exists for that and the template slice is what carries a checklist between events. Calendar delivery and overdue notification remain communications-owned work in issues #52/#66/#82; content creates no delivery rows and calls no communications surface. Calendar delivery is no longer a download only: content builds an iTIP `METHOD:REQUEST` invitation per speaker per session and hands it to communications' declared enqueue interface, which an organizer triggers from the sessions workspace, and the portal offers Google and Outlook links beside the `.ics` — asserted in `calendar-invite.test.ts` (the RFC 5545/5546 properties, parameter quoting, folding), `speaker-calendar-invites.test.ts` (one delivery per speaker per session, idempotent on an unchanged agenda, A→B→A producing three same-`UID` requests with `SEQUENCE:0`, `1`, `2`, a corrected address producing another higher sequence, refusal without `content:manage`, and the whole path composed against the real communications service), `d1-communications-repository.integration.test.ts` (the durable state and delivery advance together), `calendar-links.test.ts` and `integrations-http.test.ts`. **What that evidence does not cover is the last step: no mail client has ever rendered one of these invitations**, because the fixture provider sends no mail — so it proves the invitation is built correctly and reaches the provider, and stops there. Overdue notification remains communications-owned work in issues #66/#82. **Every single guarded `UPDATE` in `d1-content-repository.ts` whose caller read the row first and then reports a save now reads the affected-row count (issue #202, closing `GAP-025` (content) — the register allocated that id twice, and the webhook entry above it is the one that survives).** That is a description of a set rather than a rule, deliberately: three attempts at a one-line rule for this file were each published and each refuted by review, because the file has real exceptions with real reasons — `beginSpeakerImport` converges on a quiet zero by design, and `replaceLatestAsset`'s lost demotion is caught by a partial unique index that makes the batch throw, which was proven against real D1 rather than argued. The adapter's docblock enumerates every writer with the reason for each. #188 closed this for three siblings and named four that still discarded it; all four are closed here, so naming or clearing a headshot on a profile that has gone, and publishing or unpublishing an asset that has gone, answer exactly as a record that never existed does rather than returning 200 over a row that is not there. The decision the gap waited on is made and stated in `PRD-SPK-001`: a CSV import **refuses and reports** a row whose speaker vanished mid-run, rather than skipping it or failing the batch — the ledger is keyed on the normalized address, so the row stays `pending` and re-running the file converges on it. That also closed the same defect one level up, where `if (profile)` fell through to counting a deleted speaker as imported. `d1-content-repository.integration.test.ts` drives a row genuinely deleted between the caller's read and its write, one assertion per writer, **each verified to fail with its own guard removed**; `content-csv-import.test.ts` drives both halves of the import's refusal and was confirmed against the previous behaviour. The sweep across every adapter that the issue demanded found one sibling outside content — `transitionAtomically` answered with the rows it read rewritten to the new status, from two conditional statements whose counts it discarded — and found every other adapter either already on `changedRows` or guarded by a re-read. `updateProfile` and `updateSession` remain unguarded and are the only writers that are, fixture-only with no production caller, stated at both the port and the adapter. |
| `ACC-CRM` | `JNY-008` | shipped | `apps/api/test/crm-service.test.ts`, `crm-http.test.ts`, `d1-crm-repository.integration.test.ts`, `d1-speaker-conversion.integration.test.ts`; `apps/web/test/crm-owner-assignment.test.tsx`, `crm-directory.test.tsx`; `apps/web/e2e/crm.spec.ts` | The organization-wide directory (issue #97) is covered at all four levels. Its negatives are not covered evenly, and the difference matters: the plain cross-organization refusals — an organizer naming another organization, a reviewer with no `crm:manage`, an event outside the organization — are asserted at service, HTTP, D1 and browser level, but the **mixed-role escalation is a single service-level unit test** (`borrowedCapability` in `crm-service.test.ts`), because the seeded demo personas the HTTP and browser suites drive contain no identity belonging to two organizations. That third authorization condition is the one this issue added, so it is worth naming exactly how far its evidence reaches. Two limits are worth naming. **The outreach preview limitation this row used to carry is closed.** It was recorded while communications' only delivery entry point wrote, which would have made a preview queue the messages it claimed to be previewing. `prepareEnqueue` arrived with that domain's `public.ts`, so the preview resolves the template through it and an unknown key is refused on the screen showing what will be sent. `apps/api/src/application/crm/outreach-dispatch.ts` records this, and it closes when communications publishes a resolve-without-writing call (issue #66's `prepareEnqueue`). **Merging two records already sourced into the same event moves one link, not both**: the primary keeps its own and the loser's stays on the merged-away row, because `crm_contact_events` is keyed on `(contact_id, event_id)`. Nothing is deleted either way and the D1 suite asserts it. Beyond this issue: durable speaker-conversion claim rows left behind by a permanently failed workflow still have no reconciliation or alerting (`DEBT-004`), and `crm.spec.ts` remains one of the suites that accumulates fixture rows (`DEBT-007`) — the directory tests create their own stamped contacts rather than consuming the seeded duplicate pair, so they are re-runnable without a reset |
| `ACC-AGENDA` | `JNY-006` | shipped | `apps/api/test/agenda-service.test.ts`, `agenda-http.test.ts`, `d1-agenda-repository.integration.test.ts`, `agenda-backfill-parity.test.ts`, `agenda-schedule-reconciliation.test.ts`; `apps/web/test/agenda-timeslots.test.tsx`, `agenda-timezone.test.tsx`, `agenda-failure-feedback.test.tsx`, `agenda-assisted-placement.test.tsx`, `agenda-session-selection.test.tsx`, `agenda-week-board-cost.test.tsx`; `apps/web/e2e/agenda.spec.ts`, `reference-slice.spec.ts`, `lifecycle.spec.ts` | Publication is transactional, its versions are allocated per attempt, and a retried command replays through `Idempotency-Key` rather than freezing a second version, so concurrent publishes are distinct and monotonic and a failed publication leaves neither snapshot nor event. `EVT-SCHEDULE-PUBLISHED` is now durably stored in that same batch through communications' `event` channel, exactly one record per committed publication and none after a rollback, and draining it produces one schedule confirmation per reachable speaker (`DEBT-006` closed, issue #22; covered by `apps/api/test/d1-communications-repository.integration.test.ts`) Assisted placement now covers the **selected** subset as well as the whole board (issue #119): the request carries exactly what is ticked, a subset costs the same one draft read and one write as a full pass, and the browser suite drives selection by key press alone — Space ticks, Enter clears, and the group control's partly-ticked third state is asserted rather than assumed. Two limits are worth naming. There is deliberately **no range gesture** (no click-then-shift-click): the affordance is one native checkbox per session plus a select-all over whatever the search is currently listing, which is what makes "every drag has a keyboard equivalent" true here by construction rather than by a second code path. Its own evidence is hand-rolled and behavioural — roles, accessible names, Tab **reachability** as well as Space/Enter operation, the group control's indeterminate state, where focus lands after each action that disables or removes its own control, and a polite live region that is not a second `status` region, asserted in `agenda.spec.ts` and `agenda-session-selection.test.tsx`. That sits under the axe WCAG A/AA sweep issue #48 landed while this change was in review, which walks every organizer destination including `/agenda`: the rail's checkboxes, group control and live region pass it. The two are complementary — axe reports no rule violation on the rendered board, and says nothing about whether the keyboard can reach a checkbox or where focus goes when a control removes itself, which is what the assertions above exist for. The last meaningful per-session schedule revision is now stored in `agenda_session_schedules` and advanced inside the publication's own batch, so it shares the snapshot's fate and the calendar-invite read is one indexed row per session instead of a replay of every board the event has ever published (issue #141, follow-up to #136). `1601`'s backfill is tested against the rule rather than against expected numbers — the migration is replayed over a hand-built history covering an unchanged republication, an empty board, an identical return after absence, a missing slot, a removed room and a same-hour slot move, and the table it produces is asserted equal to folding `nextSessionScheduleRevisions` over the same rows — because `revision` is already written into deployed `calendar_invite_states.schedule_ref` values and a disagreement would resend an invitation to every speaker holding one. The same equivalence is asserted over 2,000 generated histories per run — 1,000 seeds carrying two events each — in `agenda-backfill-parity.test.ts`, which also asserts its generator still reaches the cases that *discriminate* the two implementations rather than merely their shapes — double placements whose copies resolve to different times or rooms, and sessions still placed by the final publication whose room the snapshot has dropped — because a review found that reversing the placement ranking and removing the room-name `COALESCE` each left the whole suite green while the fixtures had only the shapes. What the stored form gave up — the replay was self-correcting because it recomputed from immutable snapshots, and a stored answer can drift — is now detected and repaired rather than merely recorded, and `GAP-024` is closed with it (issue #169). Migration `1602` puts the detection in the database: a trigger on every insert into `agenda_publications` advances a watermark, so a writer that knows nothing about the derived table — the old Worker serving during the deploy window, an import, a fixture — still moves it, and a second watermark that only the fold writes says whether the table has caught up. An unmaintained insert cannot be made impossible, because the derived rows are a fold over the whole history that no `CHECK` can express and no trigger can see later statements of the same transaction; it can be made impossible to go unnoticed, which is what this does. **Repair happens on three paths, deliberately.** Every read re-derives a drifted answer before serving it, because what is being served decides whether a speaker is mailed an invitation to a session the programme does not schedule and whether the invitation that puts a returning talk back on their calendar is suppressed, and mail does not roll back; the one-minute tick sweeps up to twenty drifted events nobody read; and `GET`/`POST /api/events/{eventId}/agenda/schedule-reconciliation` lets an organizer ask without acting, and act on request. **The objection is named rather than left to be discovered: an automatic repair can hide the writer producing the drift.** The answer is that every repair is logged as `agenda.schedule.drift_repaired` with both watermarks and the three divergence counts — and the observer is bound to the repository rather than to the sweep, which review found was the difference between a true claim and a false one: reads repair first for every event anybody opens, so a sweep-only observer would have reported exactly the events that matter least. Two kinds of line, distinguished by the counts. A repair whose three counts are all zero is migration `1602`'s backfill claiming the watermark it deliberately left unclaimed — one per already-published event, spread over the first ticks at twenty a tick, and never again. A repair with a non-zero count is a real divergence: one is a deploy that raced a publication, a recurring one names a writer that needs fixing, and a settled deployment produces none at all. Both of `GAP-024`'s failure directions are covered because the repair replays the row *set* as well as the numbers: `d1-agenda-repository.integration.test.ts` drives a publication written directly and asserts the phantom row is dropped by the next read, that a later publication does not fold the missed one through (the session placed at v1, unplaced at v2 and returned identically at v3 takes revision **3**, which is the resend #136 exists to guarantee), that a table edited behind the watermark's back is found only by the replay, and that the affected-row count the claim rests on is refused rather than guessed when a driver omits it. `1602` deliberately backfills every already-published event as *unverified* rather than current, because a publication can land between `1601` and `1602` and a migration that claimed otherwise would put the first false statement into the table whose purpose is to be believed. **There is no console surface**: an organizer reaches the reconciliation through the API, and the automatic paths are what make that acceptable. What it still cannot see, or undo. A divergence with no console affordance is invisible to an organizer who never calls the route, and a table edited directly is caught only when somebody asks. And the repair is forward-only: `buildSpeakerInvite` emits `REQUEST` and never `CANCEL`, so an invitation already sent from a phantom row stays on the speaker's calendar after the row is dropped, and every later Send skips that session because it is no longer scheduled. Repair narrows the window in which wrong mail can be sent; it cannot retract what left during one. Two long-standing agenda debts close here as well (`DEBT-008`, `DEBT-009`): an empty board no longer names the zone abbreviation at today's DST state — it names no abbreviation, because a board with no slots shows no instant and the event record carries no dates to substitute — and the week board's day keys are read once per slot instead of once per slot per cell, asserted by holding the slots fixed and quadrupling the cells |
| `ACC-PUBLIC` | `JNY-007` | shipped | `apps/api/test/publication.test.ts`, `itinerary.test.ts`, `d1-publication-repository.integration.test.ts`, `seed-state.integration.test.ts`; `apps/web/test/publishing.test.tsx`, `public-event-pages.test.tsx`, `public-itinerary.test.ts`; `apps/web/e2e/public-event.spec.ts`, `public-itinerary.spec.ts`, `publishing.spec.ts`, `00-seed-state.spec.ts`, `lifecycle.spec.ts`; and the built-artifact audit in `lifecycle-demo.spec.ts` (`npm run test:quality`), which carries the `ACC-DEMO-SMOKE` marker rather than this one | The seeded projection remains contract-checked before any mutating spec. Organizer-editable public details and anonymous attendee itineraries are covered across all seven public routes. The quality project serves `apps/web/dist` through the Worker and runs axe WCAG A/AA over all seven public routes and four embeds, plus explicit skip-link, landmark, focus-transition, live-route, and 390px-overflow checks. The public home budget is DCL <1.5 s, transfer <300 KiB, and <12 resource requests; its pre-budget local measurement was 55 ms, 166 KiB, and 5 requests (issues #48, #84). An itinerary is held by a capability token in the URL rather than an account (`GAP-018`). |
| `ACC-INTEGRATION` | `JNY-009` | partial | `apps/api/test/communications-service.test.ts`, `communications-lifecycle.test.ts`, `communications-public.test.ts`, `communications-template.test.ts`, `communications-http.test.ts`, `provider-contract.test.ts`, `provider-configuration.test.ts`, `d1-communications-repository.integration.test.ts`; `apps/api/test/content-service.test.ts`, `review-service.test.ts` (lifecycle call sites); `apps/api/test/accelevents-sync.test.ts`, `integrations-http.test.ts`, `d1-migration-rebuild.integration.test.ts`; `apps/web/test/communications.test.tsx`, `communications-compose.test.tsx`, `accelevents-sync-panel.test.tsx`; `apps/web/e2e/communications.spec.ts` | **The journey is now proven by deliveries the product produced.** `communications.spec.ts` composes a template version in the console, sends it to the event's speakers, drives the deployed `scheduled()` entrypoint, and asserts the delivery reaches `succeeded` carrying the message with `{{speakerName}}` substituted — it asserts nothing about a seeded row. Recovery is exercised rather than described: a delivery the fixture provider genuinely refuses is retried **by clicking Retry**, and both attempts survive in the history. A queued delivery offers no Retry control and the route refuses one with `409`. Five lifecycle moments enqueue on their own — acceptance (speaker welcome plus one message per onboarding task), a requested task, reviewer assignment through both `assign` and `distribute`, an accept/decline decision, and a published schedule fanning out one confirmation per reachable speaker — asserted in `content-service.test.ts`, `review-service.test.ts` and `communications-lifecycle.test.ts`, including that repeating each action enqueues nothing further. Time-based reminders run in the same one-minute tick: an open speaker task inside the reminder window queues exactly one reminder, and repeated ticks queue nothing further because the delivery's own idempotency key is the record that it was sent — there is no bookkeeping table to fall out of step with. An organizer can compose and send from the console, each speaker getting their own delivery, attempt history and retry. Template versions are allocated by the server, so two organizers publishing the same key concurrently both succeed with consecutive versions instead of one receiving a `500`, and an explicitly named version that is taken is a typed `409`. A send carries the `audienceVersion` the organizer confirmed against, and one whose speakers changed in between — added, removed, or readdressed — is refused with nothing written, rather than reaching a different set of people than the count on screen described. Live email, Airtable and Accelevents adapters normalize success, retry, malformed and terminal outcomes identically (`provider-contract.test.ts`); `fixture` remains the default and a half-configured `live` throws rather than falling back to a fake — in the scheduled drain, not at startup, so such a deployment serves requests and simply never sends. The **inbound** Accelevents registration sync is the other half of `PRD-INT-001` and is now operable rather than an enum value: a dry run predicts exactly what an apply will do and writes nothing — not into content and not into the last-run record — an apply converges rather than duplicating, and the organizer surface shows the run, its counts, its row-level failures and a visible failed state. `accelevents-sync.test.ts` proves the preview/apply agreement, convergence on a second apply, a recorded failure when the platform is unreadable, and refusal before any outbound request for an actor without `content:manage`; `integrations-http.test.ts` proves the routes are wired, that a speaker gets 403 and an anonymous caller 401, and that an unreadable platform is a `502 UPSTREAM_UNAVAILABLE` rather than an internal error; `accelevents-sync-panel.test.tsx` proves the surface names which source answered, gates apply behind a preview, and renders the last run's failure code. **Still unproven, and each is a real gap.** **No live adapter has ever exchanged a request with its real API**: no credential exists in this repository, the contract suite stubs `fetch`, and the staging smoke in `docs/engineering/communications-providers.md` is written up but **has not run** — the suite proves our normalization, not their APIs, and Accelevents is the least certain of the three. That applies to the inbound sync too: it runs on a deterministic in-repository roster by default and its live client has never been exchanged with a real tenant, so what is proven is our handling of registrations and not that platform's API (`GAP-012`, issue #58). Every send the browser suite observes goes to the deterministic fake, so what is proven is that a correct message reaches the provider, never that a mail client rendered it. An organizer can now send the iTIP invitation itself — `speaker-calendar-invites.test.ts`, `calendar-invite.test.ts` and `d1-communications-repository.integration.test.ts` cover the client-visible payload, monotonic per-pair sequence and atomic send state, and the portal offers Google and Outlook links beside the `.ics` — but the schedule confirmation this domain fans out still carries a **link** to the `.ics` rather than the attachment, because the two landed in different pull requests and wiring them is one call plus one payload key. And **no mail client has ever rendered one of those invitations**, so what is proven there stops at the provider as well (`GAP-010`, issue #56). Returning to a previously invited time now queues another same-UID invitation with a higher `SEQUENCE` (issue #136). `ContentService.recordMessage`, behind the content workspace's "Record communication" button, still writes a `speaker_messages` row and touches no outbox. A CFP submission sends no confirmation, deliberately: the submitter address is unverified on an unauthenticated route, so a send there would be an amplification vector (`DEBT-013`). |
| `ACC-DEMO-SMOKE` | evaluator orientation across `JNY-001`–`JNY-010` | shipped | `apps/web/e2e/lifecycle-demo.spec.ts` (the six tests `npm run test:quality` runs), `00-seed-state.spec.ts`, and the full suite from a reset | The quality project starts from its own reset and audits every organizer destination, reviewer assignments, speaker resources, agenda assisted placement, all seven public routes, all four embeds, and — new here — the two signed-out surfaces a stranger actually lands on, `/` and `/signin`, before a judge sees them. Those two are held to the same bar as the public event pages: axe WCAG A/AA, one banner and one contentinfo, a skip link that is the first tab stop, focus moved to `main` on client navigation between them, and no horizontal overflow at 390px. The same test also pins the demo door in place — **twelve** of the browser spec files bootstrap by opening `/` and clicking **Continue as organizer**, directly or through their own helper, so a landing page that moved or renamed that button would take most of the suite with it. Before any spec mutation, it also asserts the seeded outstanding-speaker row's speaker, task, and due date. Axe exposed and this change fixed the missing console banner landmark and malformed shared stat definition lists; the public shell also gained a first-tab-stop skip link. The full browser suite remains re-runnable but not non-accumulating (`DEBT-007`). |
| `ACC-EVENT-TEMPLATES` | reusable event configuration | partial | `apps/api/test/event-templates.test.ts`, `event-templates-http.test.ts`, `d1-event-template-repository.integration.test.ts`, `review-template-slice.test.ts`, `agenda-template-slice.test.ts`, `publishing-template-slice.test.ts`, `content-template-slice.test.ts`, `content-http.test.ts`, `seeded-event-template.test.ts`; `apps/web/test/event-templates.test.tsx`, `event-templates-result.test.tsx`; `apps/web/e2e/event-templates.spec.ts` | **Six categories clone and the journey runs in the browser, and the verdict is still `partial` — for one reason, stated first so it is not buried: applying is not atomic across domains (`GAP-023`, `ARC-FLOW-006`), and that is a documented limit rather than a defect awaiting a fix.** What used to sit beside it no longer does. Issue #175 makes the stored per-category outcome readable and **issue #203 makes it answer the right question**: `GET /api/events/{eventId}/template-applications` reads `event_template_applications.outcome_json` back and carries, beside it, the categories the *event* still owes — folded across every application rather than read off the newest one. #175 scoped its card to the most recent application, which was a safety rule rather than tidiness: offering an older `partial` row as a whole-clone repair writes its payload over whatever configuration replaced it, because every category converges on the payload it is given. The cost was that a later application of a *different* template, or of a subset of categories, hid the card without having fixed the refused category. `outstandingConfiguration` keeps the safety property and removes the cost by making it structural: the deciding application for a category is the newest one that actually *reached* it, so a category a later application configured is not outstanding at all, and the repair offered is one version and one category rather than a whole selection — it cannot revert anything. A `skipped` category is transparent, which stops an organizer silencing an outstanding category by cloning a template that says nothing about it. `outstanding-configuration.test.ts` pins the rule, including the issue's own closure scenario — template A partial, template B in full, the refused category still reported — with three mutations of the rule each verified to fail a test; `d1-event-template-repository.integration.test.ts` drives the same scenario against real storage, so the round trip through `outcome_json` is proven rather than assumed. An organizer who never opens Event templates **is** now told: `configuration` is a sixth category in the operational inbox (`PRD-OPS-002`), the platform decision #188 deferred, and its key carries the deciding application's instant so the inbox's existing dismissal lets an organizer who repaired a category by hand say so in one click while a fresh refusal returns. Issue #177 closes the other half of the same shape: a template and its first version are one D1 batch under a `WHERE EXISTS` guard, with the version number allocated inside the insert and returned, so an interrupted capture can no longer leave a husk — an active template with no versions, holding its name against the partial unique index — and two concurrent captures no longer race each other into `UNIQUE (template_id, version)`, which was the other way this pairing failed. Confirmed by breaking the first: writing the two rows in sequence leaves exactly that husk and fails `d1-event-template-repository.integration.test.ts`. And a version card now names the person who captured it, resolved through identity, rather than the account id it is stored under. What is asserted. A template is saved from a source event as an immutable version recording which event and which person it came from, and the seeded template's payload is not hand-written — it is what the six slices actually produced, captured through the running Worker and read back out of the table, and `seeded-event-template.test.ts` hands that stored payload back to each of the six and fails when one of them can no longer read its own category, so a slice whose payload shape moves cannot leave the seed quietly stale. A preview lists every category it would copy, exclude or refuse and **writes nothing**; that promise is load-bearing rather than decorative, and the review slice was rebuilt onto a new read-only `reviewConfiguration` query after a review pass caught its original read repairing the destination's status set on the way past. Applying converges: every one of the six compares the destination against the payload before it writes and returns without writing when they already match, which is what makes "apply twice, then compare" a real assertion rather than one that has to make an exception for a version counter — asserted per slice with a spy on the storage seam, and end to end in the browser. Refusals are reported rather than thrown: a routing rule naming an unconfigured status is dropped by rule id instead of failing the form, a rubric locked by existing assignments is `incompatible` while that event's statuses still land, a slot past the destination's last day is named rather than clamped onto it, and a public address already taken is a refusal the organizer can act on. Slot times are remapped by day offset in the destination's IANA zone with the wall clock preserved, including across a DST boundary — all four documented gap and fall-back cases have assertions behind them. `body_html` and `embed_html` are re-sanitized **on import**, because a stored payload is untrusted input by the time it is applied. A template belonging to another organization answers byte-identically to an unknown id, resolved through `EventService.belongsToOrganization` and never from a request field. The exclusion sweep is driven from `table-ownership.json` against the full six-slice composition, so a table added later cannot quietly escape it. **Not covered, and each is real.** A cloned portal resource carrying an `<iframe>` is always reported `incompatible`, because the embed host allowlist is a per-request argument the organizer supplies at save time and is never stored, so an import has nothing honest to pass; the organizer re-authorizes the host in the destination. The archived-template refusal is enforced on apply and not on preview, so the console disables its own controls rather than relying on the server. And the browser spec drives the demo fixture through the deterministic providers, so what it proves is the journey, never that a real conference's configuration survived a real clone. |

| `ACC-OPS` | permission-aware operations across every domain | partial | `apps/api/test/platform-search.test.ts`, `platform-inbox.test.ts`, `platform-audit.test.ts`, `platform-attribution.test.ts`, `platform-http.test.ts`, `publication-notifications.test.ts`, `d1-platform-repository.integration.test.ts`; `apps/web/test/command-palette.test.tsx`, `platform-search.test.tsx`, `platform-inbox.test.tsx`, `platform-audit.test.tsx`; `apps/web/e2e/platform-operations.spec.ts`, and the palette audit inside `lifecycle-demo.spec.ts` | **What ships is search, the operational inbox and the unified audit timeline.** Composition is per source, not per surface: the request needs `events:read`, and each of the six inbox categories is then read through the owning domain's own public application interface under that domain's capability. A source the caller may not read is `unauthorized` and is **named on screen as omitted**; only a genuine rejection is `failed`, degrades its own section, and is logged once with the request's correlation id — `platform-http.test.ts` asserts both halves, including that an omitted section logs nothing, that the internal rejection never reaches the caller, and that a reviewer still gets an answer. Blind review is structural rather than recovered: the reviewer projection is chosen by role, so the organizer path is never called at all, and the assertion is made over what serializes, the way `ACC-REVIEW` makes it. The cross-event negative is real rather than vacuous — both seeded events hold a matching row, and the test proves the sibling's row is absent from one scope and present in the other. There is deliberately **no search index**: every hit comes from the read the console itself would make, so private CRM notes and unpublished speaker material keep their original rules by construction. **The audit timeline is append-only structurally, and that is asserted rather than described.** `d1-platform-repository.integration.test.ts` drives raw `UPDATE` and `DELETE` against real D1 and both are refused by the table's own triggers — driven as SQL rather than through the store, deliberately, because the store has no method that edits or removes a record and going through it would only prove that an API with no such method has no such method. The same suite proves a replayed idempotency key writes one row and keeps the first, that a prepared record committed in a caller's batch survives or fails with it (the failing case leaves no row), that paging is deterministic through records sharing one instant, and that the in-memory twin the service suites drive agrees with the real store. `platform-audit.test.ts` proves the rules storage cannot: a record carries the request's actor and correlation id, a consequence with no request behind it is `system` with a null id rather than an invented name, recording **never throws** and reports the record it lost with the key needed to write it by hand, and the timeline is gated on `events:settings:read` — a reviewer holding `events:read` is refused. `platform-http.test.ts` proves the idempotency key never reaches the caller. **The timeline covers five domains.** Review, content, agenda and communications are hooked at the composition root through ports that already existed, with no edit to any other domain's application code. Publishing had no such seam — a site going live was the one change nothing could account for — so it now declares a `PublicationNotificationPort` of its own, the same inversion content and review already use, and `publication-notifications.test.ts` asserts what an observer depends on: the fact is reported only after the write is durable and not at all when it fails, it carries publishing's own address and instant rather than an observer's clock, an unauthorized attempt reports nothing, and the port is optional so a composition with no observer publishes exactly as it did before. The cross-domain ordering test asserts one ordered list with the right actor and source across those domains' actions. Be exact about its reach: it re-types the calls rather than importing `index.ts`, so it proves that a firing recorder produces the record the timeline shows — not that the composition root calls it with the right arguments. The rule most likely to drift, the idempotency key, is therefore extracted as `lifecycleAuditKey` and tested directly, including that one organization's two events do not collide, and every production key now routes through it. It does not re-prove that the domains call those ports at all, because `content-service.test.ts` and `review-service.test.ts` already assert that, and the browser spec closes the loop end to end by publishing a schedule and reading the record it produced. **Records accumulate.** The table has no foreign keys — an audit record has to outlive the thing it describes — so nothing here is removed by the seed reset, which is what an append-only log does and is why the browser spec creates its own event rather than asserting against the shared one. There are **no per-record routes in the console**, so a hit opens the workspace that holds the record rather than the record itself, which the browser spec asserts as exactly that and `GAP-022` owns. Filtering is in memory over projections the console already reads, bounded per section and never paginated deeper, so the cost is proven bounded but the behaviour on an event far larger than the seed is unmeasured — also `GAP-022`. **Attribution no longer rests on two accidents** (issues #178, #179). It used to hold because `platformRoutes` happened to be listed first in the route registry and because every service happened to be constructed inside `fetch`; a reordered array and a hoisted `const` would each have produced records naming the wrong person, or nobody, with nothing failing. A route module now declares its request-scoped middleware separately and `app.ts` mounts every module's before any module's routes, so `platform-attribution.test.ts` can build an app with platform registered **last** and still see a downstream domain's mutation attributed to the request's actor — a test that fails against the old arrangement. The identity holder is opened and ended per request, so a consequence outliving its request names nobody rather than whoever was last through the door; and two requests overlapping on one holder are reported and attributed to nobody, asserted by driving two concurrent requests through one shared composition with a barrier rather than trusting the scheduler to interleave them. The inbox is now in this row too, and its central claim is that its items are **derived**: `platform-inbox.test.ts` proves an item disappears when the condition it named is resolved with no write at all (the dismissal store is spied on and never called), that a dismissal survives a re-derivation of the same occurrence and does *not* survive a moved deadline, that one organizer's dismissal never touches a colleague's list, and that a dismissal is refused for a key this event is not currently showing — including one derived on a sibling event. **The programme category's keys now carry an occurrence too** (issue #180, closing the sixth limit `GAP-022` named): the agenda maintains, inside the same optimistic write that changes the board, the revision at which each session's placements last changed and at which each time slot was last retimed, and a conflict carries the later of its two placements' and its two slots'. The deciding test drives a real `AgendaService` over the in-memory board rather than a hand-written projection — place, dismiss, unplace — because the fact is one the agenda has to produce on its *write* path, and it is paired with the negatives that matter: a dismissal survives every edit to a different session, and none of an added room, an added slot, a retiming of a slot the clash is not in, a re-spelling of the same instant, or a reordering of the slot list moves any conflict's number. Those negatives assert the conflict list is non-empty *before* asserting about it, because `every` is true of an empty array — a review pass proved the earlier form survived a mutation that dropped the whole board. Nothing is backfilled, so a dismissal recorded before the numbers existed returns open once; both repositories normalize the field on read, and `agenda-service.test.ts` parses an assisted-placement answer over a board stored without it through `agendaAssistedDraftSchema` — the path a review pass found could serve a response the console's own contract refuses. `d1-platform-repository.integration.test.ts` drives the one table this lane adds against real D1: the composite primary key makes a repeated dismissal one row that keeps its first timestamp, `restore` reports whether it removed anything, the foreign keys refuse an unknown event or actor, a driver that cannot report a row count is an error rather than a silent zero, and the in-memory twin the service suites drive is asserted to agree with the real one. Partial failure is asserted at both levels — `platform-http.test.ts` proves one failed category is logged once and correlated while the others answer, and `platform-inbox.test.tsx` proves the page does not blank. **What the seed cannot show, and why.** Four of the six categories are populated by the deterministic seed and asserted on their own content in `platform-operations.spec.ts`. Two cannot be. Publication: the seeded event is published and its draft matches its snapshot, so there is genuinely nothing awaiting publication, and #99 is not permitted to add a platform seed fragment. Rather than assert a row into existence, the browser spec creates an event and reads its inbox, which is the honest way to reach that state. **`configuration` is the sixth category (issue #203) and has no browser assertion at all**, for the same reason and one more: `seed/reset.sql` deletes `event_template_applications`, so a seeded event owes nothing, and populating it in the browser would mean applying a template that half-fails on purpose. Its evidence is `platform-inbox.test.ts` — the derived item's whole shape, its occurrence-keyed dismissal, and degradation of that section alone — plus `platform-inbox.test.tsx` for the rendered section. That is a real gap in the browser tier and is named rather than implied. No D1 integration evidence carries this marker for **search**, which adds no table and writes nothing |

Webhook evidence for `ACC-INTEGRATION`: `apps/api/test/d1-webhooks.integration.test.ts` drives the real D1 schema through subscribe → schedule-event fan-out → signed receiver failure → bounded retry success → stale-timestamp refusal → tampered-body refusal → actor-attributed manual replay. It also proves fan-out idempotency, 24-hour dual-signature rotation, keyset history, API-client organization denial, encrypted secret storage, concurrent partial-update preservation, and that receiver content is absent from history. `apps/api/test/webhook-security.test.ts` exercises the authenticated trusted-egress client contract. `apps/webhook-egress/test` exercises the checked-in enforcement foundation's mixed public/private DNS refusal, send-time rebinding refusal, pinned lookup with original-host TLS SNI, target-header allowlist, bounded normalized results and bearer overlap. No durable enforcement service is deployed or live-verified (`GAP-026`, [issue #194](https://github.com/adityak6798/ManageMyConference/issues/194)); API routes return `503 WEBHOOK_UNAVAILABLE` until it and the wrapping keys are configured, so live delivery interoperability remains unproven.

Wave 5 publishing evidence addendum (`ACC-PUBLIC`, `ACC-AGENDA`):
`d1-publication-repository.integration.test.ts` now publishes a real second agenda snapshot and
reads the public hub and schedule back from the same active publishing projection, asserting the
moved time/room/track, agenda provenance, monotonic immutable history, and the negative that agenda
publication neither creates nor reactivates an unpublished site. It also refuses a refresh result
whose D1 driver omits `meta.changes`. `publication.test.ts` proves accepted-content and CFP lifecycle
changes reconcile without another site publish while site-owned event fields remain deliberate.
`public-event-pages.test.tsx` covers speaker-aware search and combined Track/Format/Location facets;
`public-itinerary.spec.ts` carries a plan into the itinerary embed; `publishing.spec.ts` exposes all
five embed types and the JSON feed. The clean seed now contains two placed sessions across two days.
These assertions close the former two-snapshot disagreement for the paths they exercise; live cache
and deployment verification remain bounded by the existing hosted-smoke gap.

## What each row's automated evidence actually is

- Browser evidence is `apps/web/e2e/*.spec.ts`, one shared local D1 fixture, `workers: 1`.
  `apps/web/e2e/lifecycle.spec.ts` is the only spec that carries a single artifact across every
  boundary: public submission → triage → reviewer scoring → acceptance → readiness → agenda
  placement → schedule publication → site publication → public page and both embeds, then hands the
  fixture back.
- Component evidence is jsdom (`apps/web/test/*`). It stubs `fetch`, so a marker there proves
  rendering, never the pipeline. `apps/web/test/publishing.test.tsx` carries `ACC-PUBLIC` for that
  reason; `apps/web/e2e/publishing.spec.ts` is the half that touches the real API.
- API and domain evidence is `apps/api/test/*.test.ts`; D1 evidence is
  `apps/api/test/*.integration.test.ts`, run by `npm run test:d1` against Miniflare with every
  migration applied.
Where a row carries a gap, the gap is part of the record: it names what is not true yet and the
`GAP-*`/`DEBT-*` entry that owns it. Full detail is in [known gaps](known-gaps.md) and the
[technical debt register](../exec-plans/tech-debt.md); the feature-level picture is in
[competition traceability](../product/competition-traceability.md).
