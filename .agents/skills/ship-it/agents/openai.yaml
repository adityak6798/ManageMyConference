interface:
  display_name: "Ship It"
  short_description: "Ship work with CI, review, and tracked follow-ups"
  default_prompt: "Use $ship-it to take this implementation through review, CI, automated feedback, and durable deferred-work issue capture."
