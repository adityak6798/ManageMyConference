name = "project-greenroom-api"
main = "src/index.ts"
compatibility_date = "2026-08-08"

[vars]
# The ORGANIZER of every calendar invitation, when no mail sender is configured.
#
# An iTIP REQUEST must carry an ORGANIZER, and Gmail and Outlook check it corresponds to the
# sending identity — so in a real deployment this is the mail sender and `EMAIL_SENDER` takes
# precedence over it. This default exists for the configurations that send no mail at all: local
# development, CI, Playwright and the demo, where `COMMUNICATIONS_PROVIDERS=fixture` means the
# invitation reaches a deterministic fake rather than a mailbox. Without it the send refuses and
# the organizer's Send button answers 500 in every configuration this repository ships.
#
# `.invalid` is reserved by RFC 2606 and can never resolve, which is the point: a placeholder that
# cannot be mistaken for a real mailbox and cannot receive a reply if one ever escaped.
CALENDAR_ORGANIZER_EMAIL = "organizer@greenroom.invalid"

# This deployment is a demo, and says so in the only place that can enforce it.
#
# `runtimeAuth` (src/index.ts) refuses `DEMO_MODE=true` unless `ENVIRONMENT=development`, and
# refuses to boot at all without either demo mode or `AUTH_EMAIL_ENDPOINT`/`AUTH_EMAIL_TOKEN`.
# Production emailed-code sign-in does exist, but demo mode turns it
# off by design — `/api/auth/code` answers 404 while `demoMode` is set — and this deployment carries
# no mail bindings, so demo personas are the only way in here. Local development keeps these in a
# gitignored `.dev.vars`; a deployed Worker cannot read that file, so the two non-secret ones live
# here. `SESSION_SECRET` remains a Worker secret and is never written to this file.
#
# The cost, stated plainly: anyone who finds the deployed URL can sign in as an organizer of the
# seeded event. That is acceptable for a demo holding only seed data and is not acceptable for
# anything else. Graduating this deployment is issue #12 — durable logout and revocation, rotation
# and recovery, membership administration, audit events, the provider ADR — after which
# `ENVIRONMENT` becomes `production` and both of these lines are deleted.
ENVIRONMENT = "development"
DEMO_MODE = "true"

# The AI review suggestion port (#110): `fixture`, `live` or `off`.
#
# `fixture` is the adapter's own default, so this line changes nothing today — it is here to make
# the mode a visible, deliberate property of the deployment rather than an implicit one, the way
# `DEMO_MODE` is. This deployment drafts with the deterministic fake: every suggestion is a hash of
# the abstract's text, says so in its own summary, and needs no credential or network.
#
# `live` additionally requires `REVIEW_AI_API_KEY`, which is a Worker **secret** and never written
# here. A `live` mode without it does not fail the deploy and does not take the reviewer's queue
# down; it fails the Draft button and names the missing binding in the Worker log. `off` withdraws
# the assistant entirely. See docs/engineering/review-suggestions.md.
REVIEW_AI_PROVIDER = "fixture"

# `SESSION_SECRET_PREVIOUS` is a Worker **secret** and is never written here. It exists only while
# a `SESSION_SECRET` rotation is in flight: issuance always uses `SESSION_SECRET`, verification
# tries it and then this one, so rotating no longer signs every user out at the instant of the
# deploy. Set it to the old value, deploy, and delete it after one full session lifetime (8h).
# `runtimeAuth` refuses to boot when it equals `SESSION_SECRET` or is the development placeholder,
# because either is a rotation that did not happen. Procedure:
# docs/engineering/security-operations.md.

# Google sign-in (`ADR-004`): configured here.
#
# `GOOGLE_CLIENT_ID` is the OAuth client's public identifier and `GOOGLE_REDIRECT_URI` is the exact
# URI registered against that client in the Google Cloud console — for this deployment,
# `<PUBLIC_BASE_URL>/api/auth/google/callback`. Both are non-secret, which is why they belong in
# vars rather than in a secret; the redirect URI is *configuration* on purpose, never derived from
# a request, because a redirect URI a caller can name is the open redirect. It is not derived from
# `PUBLIC_BASE_URL` either: local development inherits that value from this file and would then
# claim the deployed callback as its own.
#
# `GOOGLE_CLIENT_SECRET` is a Worker **secret** (`npx wrangler secret put GOOGLE_CLIENT_SECRET`)
# and is never written here. All three or none: `resolveGoogleConfiguration` (src/index.ts) refuses
# to boot a partial configuration by name, because a deployment that offers a sign-in button it
# cannot complete is worse than one that offers none. With none of them set, `/api/auth/config`
# reports `google: false`, both Google routes answer 404, and this deployment is byte-for-byte
# the configuration it was before the feature existed.
#
# **The sequence that makes these live**, and the one to repeat if the client is ever rotated.
# They were commented out until `GAP-019` closed, because the demo restore would have erased the
# first person to sign up here; `npm run reset:demo` now reads the database before it writes and
# refuses when it finds an organization, user or event the seed does not name.
#
#   1. In the Google Cloud console, create an OAuth 2.0 **Web application** client, and register
#      exactly this redirect URI against it:
#      `https://project-greenroom-api.adityak6798.workers.dev/api/auth/google/callback`
#      It must match byte for byte; Google refuses the exchange otherwise.
#   2. Put the client's own id in `GOOGLE_CLIENT_ID` below. The redirect URI stays exactly as
#      written: it is configuration on purpose, never derived from a request (that is the open
#      redirect) and never from `PUBLIC_BASE_URL` (local development inherits that value from this
#      file and would claim the deployed callback as its own).
#   3. Deploy (`npm run deploy`, or merge to `main` and let CI do it). Note what putting the id
#      here does to *local* runs: `wrangler dev` reads this file, so a machine with no
#      `GOOGLE_CLIENT_SECRET` would hold two bindings of three and refuse every request.
#      `npm run setup:local` writes all three blank into the gitignored `.dev.vars` for exactly
#      that reason, and an empty value there overrides the one above.
#   4. `cd apps/api && npx wrangler secret put GOOGLE_CLIENT_SECRET` **immediately afterwards**,
#      pasting the client secret. Then confirm `/api/auth/config` reports `google: true` and
#      complete one real sign-in. That sign-in is also what closes `GAP-020`, which records that
#      this adapter has never exchanged a request with Google — record the date, commit and
#      client id there when it succeeds.
#
# **Steps 3 and 4 are one operation, and between them this deployment is down.** All three
# bindings are one unit and `resolveGoogleConfiguration` throws on a partial one from inside
# `fetch`, so a Worker holding two of the three answers 500 to *everything* — `/health` and the
# demo personas included, not merely Google sign-in. Nothing can make the two land atomically:
# a secret is not part of a deploy. This order is the short way round, because a `secret put` is
# seconds and a deploy is minutes; putting the secret first inverts that and leaves the
# deployment failing for the whole length of a build. If step 4 goes wrong,
# `npx wrangler secret delete GOOGLE_CLIENT_SECRET` restores service at once by returning to
# none of the three.
#
# Two things to know before step 4 rather than after. A Google identity resolving to a **seeded
# persona address** is refused and logged: `seed/reset.sql` gives the personas real-looking
# addresses, and linking one would hand a real session to whoever presses "Continue as organizer"
# next. And a self-serve signup on this deployment writes rows the demo reset will then refuse to
# delete — that is the guard working, and `npm run reset:demo` will tell you what it found and
# what destroying it would cost. See docs/engineering/security-operations.md.
#
GOOGLE_CLIENT_ID = "629474442220-ab3t4tb1bgddnsfjjkm801puan53chtu.apps.googleusercontent.com"
GOOGLE_REDIRECT_URI = "https://project-greenroom-api.adityak6798.workers.dev/api/auth/google/callback"

# The origin the scheduled outbox drain puts into links it sends.
#
# The cron handler has no request to read an origin from, so a link in a schedule confirmation
# comes from here or it comes from nowhere. Non-secret. Must match the deployed hostname; a stale
# value here produces messages whose links resolve to the wrong deployment rather than failing
# visibly.
#
# Local development inherits this, because `.dev.vars` does not set it — so a calendar link in a
# locally generated message points at the deployed demo rather than at localhost. That is cosmetic
# today, since every local provider is a deterministic fake and no message leaves the machine. Set
# `PUBLIC_BASE_URL` in `apps/api/.dev.vars` to override it if you are working on message contents.
PUBLIC_BASE_URL = "https://project-greenroom-api.adityak6798.workers.dev"

[assets]
directory = "../web/dist"
not_found_handling = "single-page-application"
run_worker_first = ["/api/*", "/health", "/openapi.json", "/docs"]

[[d1_databases]]
binding = "DB"
database_name = "manage-my-conf"
database_id = "5aa5ed70-b4f8-443a-a3a4-f3a4e41cce7b"
migrations_dir = "migrations"

[triggers]
crons = ["* * * * *"]
[[r2_buckets]]
binding = "ASSETS"
bucket_name = "manage-my-conf"
